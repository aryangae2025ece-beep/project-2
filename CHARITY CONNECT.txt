<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>CharityConnect – Donation Portal</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-r from-pink-500 via-purple-500 to-indigo-500 flex items-center justify-center">

  <!-- Donation Form Card -->
  <div id="formSection" class="bg-white p-8 rounded-2xl shadow-2xl w-full max-w-md transition-all duration-700">
    <h1 class="text-3xl font-bold text-center mb-6 text-purple-700">💖 CharityConnect 💖</h1>
    
    <form id="donationForm" class="space-y-4">
      <!-- Name -->
      <div>
        <label class="block font-semibold text-gray-700 mb-1">Full Name</label>
        <input type="text" id="name" class="w-full border border-gray-300 rounded-lg p-2 focus:outline-none focus:ring-2 focus:ring-purple-400" placeholder="Enter your name" />
      </div>
      
      <!-- Email -->
      <div>
        <label class="block font-semibold text-gray-700 mb-1">Email</label>
        <input type="email" id="email" class="w-full border border-gray-300 rounded-lg p-2 focus:outline-none focus:ring-2 focus:ring-purple-400" placeholder="Enter your email" />
      </div>
      
      <!-- Donation Amount -->
      <div>
        <label class="block font-semibold text-gray-700 mb-1">Donation Amount (₹)</label>
        <input type="number" id="amount" min="1" class="w-full border border-gray-300 rounded-lg p-2 focus:outline-none focus:ring-2 focus:ring-purple-400" placeholder="Enter amount" />
      </div>

      <!-- Submit Button -->
      <button type="submit" class="w-full bg-purple-600 hover:bg-purple-700 text-white font-semibold py-2 rounded-lg transition duration-200">Donate Now</button>
    </form>

    <!-- Thank-you Message -->
    <div id="thankYouMsg" class="hidden text-center mt-6 text-lg font-semibold text-green-600"></div>

    <!-- Total Donations -->
    <div id="totalDisplay" class="hidden mt-4 text-center text-gray-700 font-semibold"></div>
  </div>

  <!-- Success Section -->
  <div id="successSection" class="hidden bg-white p-8 rounded-2xl shadow-2xl text-center w-full max-w-md transition-all duration-700">
    <h1 class="text-3xl font-bold text-green-600 mb-4">🎉 Donation Successful!</h1>
    <p class="text-gray-700 text-lg mb-6">Thank you for your generous support. Your kindness makes a difference! 💖</p>
    <button id="backBtn" class="bg-green-500 hover:bg-green-600 text-white px-6 py-2 rounded-lg">Go Back</button>
  </div>

  <script>
    const form = document.getElementById('donationForm');
    const thankYouMsg = document.getElementById('thankYouMsg');
    const totalDisplay = document.getElementById('totalDisplay');
    const successSection = document.getElementById('successSection');
    const formSection = document.getElementById('formSection');
    const backBtn = document.getElementById('backBtn');

    // Array to store donation amounts
    let donations = [];

    form.addEventListener('submit', (e) => {
      e.preventDefault();

      const name = document.getElementById('name').value.trim();
      const email = document.getElementById('email').value.trim();
      const amount = parseFloat(document.getElementById('amount').value);

      // Validate inputs
      if (!name || !email || isNaN(amount) || amount <= 0) {
        alert('⚠ Please fill out all fields correctly.');
        return;
      }

      // Add donation to array
      donations.push(amount);

      // Calculate total donations
      const total = donations.reduce((sum, val) => sum + val, 0);

      // Display thank-you message and total
      thankYouMsg.classList.remove('hidden');
      thankYouMsg.innerText = 🎉 Thank you, ${name}, for donating ₹${amount.toFixed(2)}!;
      totalDisplay.classList.remove('hidden');
      totalDisplay.innerText = 💰 Total Donations: ₹${total.toFixed(2)};

      // Clear form fields
      form.reset();

      // Simulate redirect after 3 seconds
      setTimeout(() => {
        formSection.classList.add('hidden');
        successSection.classList.remove('hidden');
      }, 3000);
    });

    // Go back to donation form
    backBtn.addEventListener('click', () => {
      successSection.classList.add('hidden');
      formSection.classList.remove('hidden');
    });
  </script>

</body>
</html>